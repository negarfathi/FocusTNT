#include <limits.h>
#include <stdint.h>
/* Generated by Frama-C */

extern long long __VERIFIER_nondet_int(void);

long long wrap_around(long long value, long long lower_bound, long long upper_bound) {
    long long range = upper_bound - lower_bound + 1;
    if (value > upper_bound) {
        return lower_bound + (value - upper_bound - 1) % range;
    }
    else if (value < lower_bound) {
        return upper_bound - (lower_bound - value - 1) % range;
    }
    return value;
}
long long gettablesize_slice_1(void)
{
  long long __retres;
  long long i = 50000;
  i = wrap_around(i % (99999), INT_MIN, INT_MAX);
  if (i < 0)
{ __retres = wrap_around(- i, INT_MIN, INT_MAX);
} else __retres = i;
  return __retres;
}

void main(void)
{
  long long i;
  long long tmp;
  tmp = gettablesize_slice_1();
  long long max_fds = (unsigned long)tmp;
  i = (unsigned short)0;
  while ((unsigned long)i < max_fds)
{ i = (unsigned short)((int)i + 1);
}
  return;
}


