/* Generated by Frama-C */
struct NNode {
   struct NNode *nxtact ;
};
typedef struct NNode INSDS;

extern int __VERIFIER_nondet_int(void);

INSDS *initLink_slice_1(int n)
{
  int i;
  INSDS *head = malloc(sizeof(INSDS));
  head->nxtact = head;
  INSDS *cyclic = head;
  i = 2;
  while (i <= n) {
    {
      INSDS *body = malloc(sizeof(INSDS));
      body->nxtact = body;
      cyclic->nxtact = body;
      cyclic = cyclic->nxtact;
    }
    i ++;
  }
  cyclic->nxtact = cyclic;
  return head;
}

void main(void)
{
  int num = 100000;
  if (num <= 0) goto return_label;
  else 
    if (num > 65534) goto return_label;
  INSDS *list = initLink_slice_1(num);
  INSDS *ip = list;
  while (ip != (INSDS *)0) {
    INSDS *nxt = ip->nxtact;
    if (ip == nxt) {
      
      break;
    }
    ip = nxt;
  }
  return_label: return;
}


