/* Generated by Frama-C */
struct NNode {
   int h ;
   struct NNode *next ;
};
typedef struct NNode Node;

extern int __VERIFIER_nondet_int(void);

Node *initLink_slice_1(int n)
{
  int i;
  Node *head = malloc(sizeof(Node));
  head->next = (struct NNode *)0;
  Node *cyclic = head;
  i = 2;
  while (i <= n) {
    {
      Node *body = malloc(sizeof(Node));
      body->next = (struct NNode *)0;
      cyclic->next = body;
      cyclic = cyclic->next;
    }
    i ++;
  }
  cyclic->next = head;
  return head;
}

Node *findEnd_slice_1(Node *list)
{
  Node *begin = list;
  while (begin->next != list) begin = begin->next;
  return begin;
}

void main(void)
{
  int num = -5;
  if (num <= 0) goto return_label;
  else 
    if (num > 65534) goto return_label;
  Node *list = initLink_slice_1(num);
  Node *end = findEnd_slice_1(list);
  return_label: return;
}


