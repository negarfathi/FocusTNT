#include <limits.h>
#include <stdint.h>
/* Generated by Frama-C */

extern long long __VERIFIER_nondet_uint(void);

long long wrap_around(long long value, long long lower_bound, long long upper_bound) {
    long long range = upper_bound - lower_bound + 1;
    if (value > upper_bound) {
        return lower_bound + (value - upper_bound - 1) % range;
    }
    else if (value < lower_bound) {
        return upper_bound - (lower_bound - value - 1) % range;
    }
    return value;
}
void main(void)
{
  long long __retres;
  long long pathlen = 1000;
  long long newbufsize = 1024;
  long long len = 0;
  if (wrap_around((newbufsize + len) + (unsigned int)1, 0, UINT_MAX) == 0xffffffff) {
    __retres = 0;
    goto return_label;
  }
  if (newbufsize == (unsigned int)0) {
    __retres = 0;
    goto return_label;
  }
  while (newbufsize < wrap_around((pathlen + len) + (unsigned int)1, 0, UINT_MAX))
{ 
    if (newbufsize >= 0x80000000)
{ newbufsize = 0xffffffff;
}
    else newbufsize = wrap_around(newbufsize * ((unsigned int)2), 0, UINT_MAX);
}
  return_label: return;
}


