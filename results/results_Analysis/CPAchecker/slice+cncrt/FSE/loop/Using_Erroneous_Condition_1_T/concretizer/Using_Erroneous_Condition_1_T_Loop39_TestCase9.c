/* Generated by Frama-C */
struct Node {
   int size ;
   int selected ;
};
typedef struct Node Node;

extern int __VERIFIER_nondet_int(void);

void main(void)
{
  int direction;
  Node *menu = malloc(sizeof(Node));
  menu->selected = -2;
  menu->size = 5;
  if (menu->selected <= -2) goto return_label;
  else 
    if (menu->size < 1) goto return_label;
  int pos = menu->selected;
  int action_id = -1;
  if (action_id > 0) {
    if (pos >= 1) {
      pos --;
      direction = -1;
    }
    else goto _LAND;
  }
  else {
    _LAND: {
             pos ++;
             direction = 1;
           }
  }
  pos %= menu->size;
  int start = pos;
  while (1) {
    pos += direction;
    if (pos == menu->size) pos = 0;
    else 
      if (pos < 0) pos = menu->size - 1;
    if (! (pos != start)) break;
  }
  return_label: return;
}


